{
  "name": "socket-p5-test",
  "version": "0.0.1",
  "description": "socket-p5",
  "dependencies": {
    "express": "^4.16.3",
    "serve": "^10.0.2",
    "socket.io": "^2.1.1"
  },
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
